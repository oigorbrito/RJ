# Blocker register

Record only external conditions that prevent execution. A blocker never converts unexecuted work into PASS.

| blocker_id | type | blocked operation | observed evidence | impact | work that can continue | objective unblock condition |
| --- | --- | --- | --- | --- | --- | --- |
| RJ-BLK-001 | local environment | Local restore/build/test and executable-level CLI verification | Direct inspection of the current execution container found `git 2.47.3`, but no RJ checkout under the available working/storage roots, no `dotnet` executable or `/usr/share/dotnet` installation, and no `msbuild`, `csc`, `mcs`, or `mono`. `gh` is not installed. A non-admin SDK bootstrap attempt cannot proceed because the container cannot resolve `dot.net` (`curl: (6) Could not resolve host: dot.net`). | Local compilation and runtime test outcomes cannot be classified PASS/FAIL | GitHub-side static review, source edits, test definitions, documentation and remote evidence inspection | Expose or mount the RJ checkout in an environment with the repository-required .NET SDK, or provide network/package access sufficient to install that SDK and repository access sufficient to materialize the private checkout |
| RJ-BLK-002 | GitHub Actions runner provisioning | Remote workflow steps, including build-test | Run `33900668909` on HEAD `3a5b5cd37bdf7749f6e040e6c5dd0375ad1ee0ea` failed in `runner-smoke` before exposing any steps/logs and skipped `build-test`; an isolated rerun of the failed job produced attempt 2 and reproduced the same `runner-smoke` pre-step failure with `build-test` skipped. Prior runs show the same pattern, including `steps=[]`/`runner_id=0`. | No remote compilation or test execution is available; workflow conclusion must not be interpreted as a code FAIL | Source review, implementation, test definitions, documentation and CI metadata inspection | `runner-smoke` executes at least its first configured step and produces step/log evidence on a hosted runner |
| RJ-BLK-003 | external legal corpus/oracle | Real legal corpus admission, benchmark execution and empirical calibration against representative legal data | No authorized real legal corpus plus reviewed oracle artifacts with versioned references/hashes has been supplied to the project | Real-corpus quality results and empirical calibration of retrieval/generation/operational thresholds cannot be classified PASS | Deterministic harness, admission protocol, synthetic/self-test fixtures, structural hardening and documentation | Supply authorized legal source artifacts and independently reviewed oracle artifacts with the references, versions and exact hashes required by the corpus-admission protocol |
