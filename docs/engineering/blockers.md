# Blocker register

Record only external conditions that prevent execution. A blocker never converts unexecuted work into PASS.

| blocker_id | type | blocked operation | observed evidence | impact | work that can continue | objective unblock condition |
| --- | --- | --- | --- | --- | --- | --- |
| RJ-BLK-001 | local environment | Local restore/build/test and executable-level CLI verification | Direct inspection of the current Debian 13 (trixie) execution container found `git 2.47.3`, but no RJ checkout under the available working/storage roots, no `dotnet` executable or `/usr/share/dotnet` installation, and no `msbuild`, `csc`, `mcs`, or `mono`. `gh` is not installed. No `dotnet-sdk` package is exposed by the current APT cache/policy, `/var/cache/apt/archives` contains no .NET SDK/runtime package, and no SDK tarball/zip was found under `/opt`, `/usr/local`, `/tmp`, or `/mnt/data`. DNS resolution is unavailable for `github.com`, `api.github.com`, `deb.debian.org`, `packages.microsoft.com`, and `dot.net`, so neither repository materialization nor SDK/package bootstrap can proceed from this container. | Local compilation and runtime test outcomes cannot be classified PASS/FAIL from this execution container | GitHub-side static review, source edits, test definitions, documentation and remote evidence inspection | Expose or mount the RJ checkout in an environment with the repository-required .NET SDK, or provide working DNS/network/package access sufficient to install that SDK and repository access sufficient to materialize the private checkout |
| RJ-BLK-002 | GitHub Actions runner provisioning | Remote workflow steps, including build-test | The blocker was historically cleared by run `33934362365`, which executed runner-smoke, PostgreSQL initialization, restore, build and tests. It has recurred repeatedly. On exact Wave D gate HEAD `2b378f7cce4254afb67a76346ab9ede125dfb5f1`, run `34563704868` completed `runner-smoke` as failure with `steps = null` and `logs_url = null`; dependent `build-test` was skipped. No checkout, .NET setup, PostgreSQL service, migration, build or test step executed. | Exact-head remote compilation, migration, PostgreSQL integration and HTTP restart-recovery tests are BLOCKED; the workflow conclusion must not be interpreted as product FAIL | Source review, implementation, test definitions, documentation, local exact-head execution and CI metadata inspection | A new run on the exact Wave D head (or its successor) executes at least the first configured `runner-smoke` step and then exposes repository build/test steps or logs |
| RJ-BLK-003 | external legal corpus/oracle | EVAL-010 real-corpus admission and downstream empirical retrieval/generation evaluation | Wave G now implements a frozen 30–50 case manifest contract, exact manifest/source/oracle/oracle-review SHA-256 verification, independent oracle-review metadata, an external corpus verifier, and a hard oracle-isolation gate against benchmark evidence. No authorized 30–50 case source corpus plus structured oracle and independent review artifacts has been supplied. | Real-corpus admission, benchmark execution and empirical treatment selection remain NOT_TESTED; no cross-case/cross-tribunal quality claim is permitted | Contract tests, admission tooling, oracle-isolation enforcement, deterministic gates, documentation and synthetic/self-test fixtures | Supply an authorized frozen manifest containing 30–50 unique cases plus immutable source, structured-oracle and independent oracle-review artifacts with exact hashes/references, distinct author/reviewer IDs and review timestamps; publish the exact manifest SHA-256; set `RJ_EVAL010_MANIFEST_PATH`, `RJ_EVAL010_MANIFEST_SHA256` and `RJ_EVAL010_ARTIFACT_ROOT` (plus optional `RJ_EVAL010_BENCHMARK_CATALOG_PATH`) and execute `scripts/test-rjudi-wave-g.ps1` with verifier exit code `0` |
| BLK-POSTGRES-001 | local runtime dependency | Local PostgreSQL integration and HTTP contract runtime validation | `RJ_POSTGRES_CONNECTION` is absent from the current local execution environment; prior full-suite execution reported PostgreSQL-dependent skips. Wave D now provides `scripts/test-rjudi-wave-d.ps1`, which fails closed with exit code `2` when the connection is unavailable. | Local PostgreSQL-backed persistence, migration and restart-recovery behavior remain NOT_TESTED in this environment | Deterministic non-PostgreSQL tests, static review, persistence implementation, test definitions, documentation and remote CI inspection | Provide a reachable PostgreSQL connection string through `RJ_POSTGRES_CONNECTION`, then execute `scripts/test-rjudi-wave-d.ps1` on the exact Wave D HEAD |
| SRC-003 | external DataJud response evidence | Authentic DataJud response validation and promotion from typed partial observation to an admitted enrichment source | The current CNJ wiki now documents the public endpoint/alias shape, API-key authentication and the fields consumed by `DataJudPublicApiContract`. Wave F implements the documented query/parser contract, fail-closed tests and `RJ.DataJudFixtureVerifier`. However, no authentic raw DataJud response has yet been captured and admitted for this project. A 2026-09-11 attempt from the available execution container to contact `api-publica.datajud.cnj.jus.br` failed at DNS resolution before any HTTP request reached CNJ. | The documented structural contract can be implemented and tested, but live-response compatibility and a complete DataJud enrichment/merge path remain NOT_TESTED/NOT_DEMONSTRATED | Contract parser, deterministic query construction, source hashing/provenance, fixture verifier, schema-contract tests, documentation and downstream merge-policy design review | Capture an authorized authentic DataJud raw response for a known CNJ, preserve its source reference and observed-at timestamp, set `RJ_DATAJUD_FIXTURE_PATH`, `RJ_DATAJUD_EXPECTED_CNJ`, `RJ_DATAJUD_SOURCE_REFERENCE`, `RJ_DATAJUD_OBSERVED_AT`, and execute `scripts/test-rjudi-wave-f.ps1` on the exact Wave F HEAD with verifier exit code `0` |
| ATT-001 | external attachment evidence | Attachment acquisition, parser selection, OCR execution and binary-to-text end-to-end validation | The admitted real case fixture contains attachment metadata only; no authorized attachment binary plus independently verifiable extracted-text reference is available. | Acquisition/parser/OCR behavior and binary-content fidelity remain NOT_TESTED | Metadata/content separation, admission, hashing, deterministic chunking, provenance and GenerationContext integration | Supply an authorized attachment binary (or equivalent immutable artifact), expected extraction evidence and exact content hash/reference suitable for a frozen attachment protocol |
